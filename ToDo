Fix things like expand variables and history search to include the 
character under the cursor.

2 commands with the same name should not be ambiguous.

Check arguments for limits limit file 6000M

Glob bindkey's args... Are other builtins for which args are not globbed
and should be?

fix keymaps so they handle ^@ [NULL's] in key strings

make %N in prompt print the effective user.

add a bindkey -C that does not redisplay the prompt.

cd var works, but cd envvar does not

NLS catalogs for:
    function descriptions		ed.defns.c
    terminal capability names		ed.screen.c

christos
