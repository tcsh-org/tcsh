# $tcsh: Makefile.in,v 1.1 2009/10/17 17:08:40 christos Exp $

LOCALES=	C et finnish french german greek italian ja pl russian \
		spanish ukrainian
GENCAT=		gencat
INSTALL=	ginstall

datarootdir=@datarootdir@
CATALOGS=$(LOCALES:%=%.cat)
INSTALLED=$(LOCALES:%=@localedir@/%/LC_MESSAGES/tcsh.cat)

all:	$(CATALOGS)

install:	$(INSTALLED)

catalogs: all

@localedir@/%/LC_MESSAGES/tcsh.cat:	%.cat
	mkdir -p $(@D)
	$(INSTALL) $< $@

%.cat:
	$(GENCAT) $@ $(@:%.cat=%)/*set*

clean:
	$(RM) $(CATALOGS)
